services:
  postgres:
    image: postgres:15-alpine
    container_name: medilab-postgres
    environment:
      POSTGRES_DB: medilab_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - medilab-network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: .
    ports:
      - "8080:8080"
      - "5005:5005"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY:-placeholder}
      - DB_URL=jdbc:postgresql://postgres:5432/medilab_db
      - DB_USERNAME=postgres
      - DB_PASSWORD=password
      - RABBITMQ_HOST=rabbitmq
      - RABBITMQ_PORT=5672
      - RABBITMQ_USERNAME=guest
      - RABBITMQ_PASSWORD=guest
      - JAVA_TOOL_OPTIONS=-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5005 -Dliquibase.duplicateFileMode=WARN
      - LIQUIBASE_DUPLICATE_FILE_MODE=WARN
      - MINIO_ENDPOINT=http://minio:9000
      - MINIO_ACCESS_KEY=minioadmin
      - MINIO_SECRET_KEY=minioadmin
      - MINIO_BUCKET_NAME=medilab-reports
    # volumes:
    #   - ./target/classes:/app/classes
    #   - ./src/main/resources:/app/resources
    # DevTools will handle reactivation when these volumes are updated.
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_started
      minio:
        condition: service_healthy
    networks:
      - medilab-network

  notification:
    build:
      context: ./services/notification-service
    ports:
      - "8081:8081"
    environment:
      - RABBITMQ_HOST=rabbitmq
      - RABBITMQ_PORT=5672
      - RABBITMQ_USERNAME=guest
      - RABBITMQ_PASSWORD=guest
      - MAIL_HOST=smtp.gmail.com
      - MAIL_PORT=587
      - MAIL_USERNAME=ko.mail.sender@gmail.com
      - MAIL_PASSWORD=wscidfgsuzgtmmuo
      - JAVA_TOOL_OPTIONS=-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5006
    volumes:
      - ./services/notification-service/target/classes:/app/classes
      - ./services/notification-service/src/main/resources:/app/resources
    depends_on:
      - rabbitmq
    networks:
      - medilab-network

  frontend:
    image: node:20-alpine
    working_dir: /app
    ports:
      - "80:3000"
    volumes:
      - ../medilab-pro-frontend:/app
      - /app/node_modules # Avoid overwriting container node_modules with host ones if they differ (optional but good)
    environment:
      - VITE_BACKEND_URL=http://localhost:8080
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    depends_on:
      - backend
    networks:
      - medilab-network

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: medilab-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest
    networks:
      - medilab-network

  minio:
    image: minio/minio:latest
    container_name: medilab-minio
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      - MINIO_ROOT_USER=minioadmin
      - MINIO_ROOT_PASSWORD=minioadmin
    volumes:
      - minio-data:/data
    command: server /data --console-address ":9001"
    networks:
      - medilab-network
    healthcheck:
      test: [ "CMD", "mc", "ready", "local" ]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  medilab-network:
    driver: bridge

volumes:
  postgres-data:
  minio-data:
